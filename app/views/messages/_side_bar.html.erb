   <div class="side-head">
    <div class="head-name">
     <a href="#">Tom</a>
    </div>
    <div class="room-create">
     <%= link_to "チャットを作成する", '/rooms/new' %>
    </div>
   </div>

   <div class="side-body">
    <% current_user.rooms.each do |room| %>
     <div class="room">
      <div class="room-name">
       <%= link_to room.name, room_messages_path(room) %>
      </div>
     </div>
    <% end %>
   </div>